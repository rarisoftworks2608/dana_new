{
  "name": "dana-supplier-technology-day-2026",
  "version": "1.0.0",
  "private": true,
  "description": "Event Registration & QR Attendance Management System for Dana Supplier Technology Day 2026",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run dev:client\" \"npm run dev:server\"",
    "build:client": "npm run build --prefix client",
    "start:server": "npm start --prefix server",
    "db:migrate": "npm run migrate --prefix server",
    "db:seed": "npm run seed:admin --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
